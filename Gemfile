source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"

# Plugins (already included in github-pages, but explicit for clarity)
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
  gem "jekyll-paginate"
end
